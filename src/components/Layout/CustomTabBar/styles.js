import {StyleSheet} from 'react-native'

export const styles = StyleSheet.create({
  contentBottomTab: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#fff',
    paddingTop: 16,
  },
  itemTab: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    position: 'relative',
  },
})
